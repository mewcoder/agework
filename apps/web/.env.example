# 可选项
# 下面配置都有代码默认值，不需要时保持注释即可。

# 前端页面/路由/静态资源路径，默认跟随 AGEWORK_CONTEXT，例: /agent
# VITE_APP_BASE_PATH=/agent

# 前端 API 上下文，默认跟随 AGEWORK_CONTEXT，例: /agent -> /agent/api/v1
# VITE_APP_API_CONTEXT=/agent
