{
  "name": "@agework/worker",
  "version": "0.0.1",
  "private": true,
  "main": "./src/worker.ts",
  "exports": {
    ".": "./src/worker.ts",
    "./runner": "./src/runner.ts"
  },
  "scripts": {
    "prepare:codex-types": "node ../adapters/scripts/generate-codex-types.mjs",
    "prepare:test": "pnpm prepare:codex-types && pnpm --filter @agework/shared build",
    "typecheck": "pnpm prepare:codex-types && tsc --noEmit",
    "test": "pnpm prepare:test && vitest run"
  },
  "dependencies": {
    "@agework/adapters": "workspace:*",
    "@agework/shared": "workspace:*",
    "rxjs": "7.8.1"
  },
  "devDependencies": {
    "@swc/core": "^1.15.40",
    "@types/node": "^24.0.0",
    "typescript": "catalog:",
    "unplugin-swc": "^1.5.9",
    "vitest": "^4.1.8"
  }
}
